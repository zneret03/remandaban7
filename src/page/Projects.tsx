import React from "react";
import { Layout, ComponentProjects } from "../components";

const Projects: React.FC = () => {
  return (
    <Layout>
      <ComponentProjects />
    </Layout>
  );
};

export default Projects;
