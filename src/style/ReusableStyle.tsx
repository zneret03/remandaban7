import { css } from "styled-components";

const ReusableCSS = {
  header1: css`
    font-weight: lighter;
    text-transform: uppercase;
    letter-spacing: var(--letter-spacing);
  `,
};

export { ReusableCSS };
